apiVersion: v1
kind: Service
metadata:
  name: chemvr-service
  namespace: chemvr
  labels:
    app: chemvr
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: chemvr
